## Summary

- URI versioning enabled via `VersioningType.URI` with global prefix `/api` and default version `v1`
- All existing endpoints now available at `/api/v1/<path>` without any controller changes
- `DeprecationMiddleware` returns 404 for unsupported versions with clear error messages

Closes #742
