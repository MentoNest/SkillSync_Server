# =============================================================================
# SkillSync Server Configuration
# =============================================================================

# Environment
NODE_ENV=development

# Server
>>>>>>> baad43246a6fc8b870e1b4fd64007446b47c45c5
PORT=3000
API_PREFIX=api
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

<<<<<<< HEAD
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-database-password
DB_NAME=skillsync_db
DB_SSL=true

# Reverse Proxy Configuration
# Set to true if behind Nginx, CloudFlare, AWS ALB, etc.
TRUST_PROXY=true

# CORS Configuration - WHITELIST YOUR FRONTEND DOMAINS
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_METHODS=GET,POST,PUT,PATCH,DELETE
CORS_CREDENTIALS=true

# Cookie Security Configuration
# Secure: Only send cookies over HTTPS (auto-enabled in production)
COOKIE_SECURE=true
# HttpOnly: Prevent JavaScript access to cookies (prevent XSS)
COOKIE_HTTP_ONLY=true
# SameSite: CSRF protection (strict recommended)
COOKIE_SAME_SITE=strict
# Cookie domain (optional, for subdomain sharing)
# COOKIE_DOMAIN=.yourdomain.com
# Cookie expiration in milliseconds (default: 1 hour)
COOKIE_MAX_AGE=3600000

# Redis config
REDIS_HOST=redis-14303.c282.east-us-mz.azure.cloud.redislabs.com
REDIS_PORT=14303
REDIS_PASSWORD=your-secure-redis-password
REDIS_DB=0


#Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_secret


# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true

# Global rate limiting (applies to all requests)
# Production default: 50 req/min (stricter)
RATE_LIMIT_GLOBAL_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_MAX=50

# Per-IP rate limiting (unauthenticated users)
# Production default: 10 req/min (very strict)
RATE_LIMIT_PER_IP_WINDOW_MS=60000
RATE_LIMIT_PER_IP_MAX=10

# Per-wallet rate limiting
RATE_LIMIT_PER_WALLET_WINDOW_MS=60000
RATE_LIMIT_PER_WALLET_MAX=50

# Per-user rate limiting (authenticated users)
# Production default: 50 req/min (strict)
RATE_LIMIT_PER_USER_WINDOW_MS=60000
RATE_LIMIT_PER_USER_MAX=50

# Predefined rate limits
RATE_LIMIT_STRICT_MAX=10
RATE_LIMIT_RELAXED_MAX=1000

# Paths to exempt from rate limiting (comma-separated)
RATE_LIMIT_EXEMPT_PATHS=/health,/health/redis

# JWT Configuration - REQUIRED IN PRODUCTION (min 32 characters)
# Generate a strong random secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters-long
JWT_EXPIRES_IN=1h

# Mail Configuration (SMTP)
MAIL_SENDER=noreply@yourdomain.com
MAIL_SUBJECT_PREFIX=[SkillSync]
MAIL_APP_NAME=SkillSync
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_SECURE=false
OTP_TTL_MINUTES=10
OTP_SUBJECT=Your One-Time Password (OTP)

# Swagger UI Configuration
# Set to true to enable in production (NOT RECOMMENDED - use only for debugging)
SWAGGER_ENABLED=false

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
DATABASE_NAME=skillsync
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false
DATABASE_SSL=false
DATABASE_POOL_SIZE=10

# =============================================================================
# JWT Configuration
# =============================================================================

JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-chars
JWT_EXPIRES_IN=24h
JWT_ACCESS_EXPIRATION=24h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-minimum-32
JWT_REFRESH_EXPIRES_IN=7d
>>>>>>> baad43246a6fc8b870e1b4fd64007446b47c45c5

# =============================================================================
# Redis Configuration
# =============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=skillsync
REDIS_CONNECT_TIMEOUT=10000

# =============================================================================
# Feature Flags
# =============================================================================

FEATURE_ENABLE_SWAGGER=true
FEATURE_ENABLE_CACHE=true
FEATURE_ENABLE_RATE_LIMITING=true
FEATURE_ENABLE_LOGGING=true

# =============================================================================
# Seed Configuration
# =============================================================================

# Set to 'true' to disable automatic seeding on startup
DISABLE_SEED=false

# Admin wallet address for the default admin user
# This should be a valid Ethereum-compatible wallet address
DEFAULT_ADMIN_WALLET=0x742d35Cc6634C0532925a3b844Bc2e7c6A9A8B0F

# =============================================================================
# Logging
# =============================================================================

LOG_LEVEL=log
LOG_FORMAT=simple
# =============================================================================
# SkillSync Server Configuration
# =============================================================================

# Environment
NODE_ENV=development

# Server
PORT=3000
API_PREFIX=api
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-database-password
DB_NAME=skillsync_db
DB_SSL=false

# Reverse Proxy Configuration
# Set to true if behind Nginx, CloudFlare, AWS ALB, etc.
TRUST_PROXY=true

# CORS Configuration - WHITELIST YOUR FRONTEND DOMAINS
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_METHODS=GET,POST,PUT,PATCH,DELETE
CORS_CREDENTIALS=true

# Cookie Security Configuration
# Secure: Only send cookies over HTTPS (auto-enabled in production)
COOKIE_SECURE=true
# HttpOnly: Prevent JavaScript access to cookies (prevent XSS)
COOKIE_HTTP_ONLY=true
# SameSite: CSRF protection (strict recommended)
COOKIE_SAME_SITE=strict
# Cookie domain (optional, for subdomain sharing)
# COOKIE_DOMAIN=.yourdomain.com
# Cookie expiration in milliseconds (default: 1 hour)
COOKIE_MAX_AGE=3600000

# =============================================================================
# Redis Configuration
# =============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=skillsync

# =============================================================================
# Cloudinary Configuration
# =============================================================================

CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_secret

# =============================================================================
# JWT Configuration
# =============================================================================

# JWT Configuration - REQUIRED IN PRODUCTION (min 32 characters)
# Generate a strong random secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters-long
JWT_EXPIRES_IN=1h
JWT_ACCESS_EXPIRATION=24h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-minimum-32
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# Rate Limiting Configuration
# =============================================================================

RATE_LIMIT_ENABLED=true

# Global rate limiting (applies to all requests)
# Production default: 50 req/min (stricter)
RATE_LIMIT_GLOBAL_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_MAX=50

# Per-IP rate limiting (unauthenticated users)
# Production default: 10 req/min (very strict)
RATE_LIMIT_PER_IP_WINDOW_MS=60000
RATE_LIMIT_PER_IP_MAX=10

# Per-wallet rate limiting
RATE_LIMIT_PER_WALLET_WINDOW_MS=60000
RATE_LIMIT_PER_WALLET_MAX=50

# Per-user rate limiting (authenticated users)
# Production default: 50 req/min (strict)
RATE_LIMIT_PER_USER_WINDOW_MS=60000
RATE_LIMIT_PER_USER_MAX=50

# Predefined rate limits
RATE_LIMIT_STRICT_MAX=10
RATE_LIMIT_RELAXED_MAX=1000

# Paths to exempt from rate limiting (comma-separated)
RATE_LIMIT_EXEMPT_PATHS=/health,/health/redis

# =============================================================================
# Mail Configuration (SMTP)
# =============================================================================

MAIL_SENDER=noreply@yourdomain.com
MAIL_SUBJECT_PREFIX=[SkillSync]
MAIL_APP_NAME=SkillSync
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_SECURE=false
OTP_TTL_MINUTES=10
OTP_SUBJECT=Your One-Time Password (OTP)

# =============================================================================
# Feature Flags
# =============================================================================

FEATURE_ENABLE_SWAGGER=true
FEATURE_ENABLE_CACHE=true
FEATURE_ENABLE_RATE_LIMITING=true
FEATURE_ENABLE_LOGGING=true

# Swagger UI Configuration
# Set to true to enable in production (NOT RECOMMENDED - use only for debugging)
SWAGGER_ENABLED=false

# =============================================================================
# Seed Configuration
# =============================================================================

# Set to 'true' to disable automatic seeding on startup
DISABLE_SEED=false

# Admin wallet address for the default admin user
DEFAULT_ADMIN_WALLET=0x742d35Cc6634C0532925a3b844Bc2e7c6A9A8B0F

# =============================================================================
# Logging
# =============================================================================

LOG_LEVEL=log
LOG_FORMAT=simple
=======
# =============================================================================
# SkillSync Server Configuration
# =============================================================================

# Environment
NODE_ENV=development

# Server
>>>>>>> baad43246a6fc8b870e1b4fd64007446b47c45c5
PORT=3000
API_PREFIX=api
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

<<<<<<< HEAD
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-database-password
DB_NAME=skillsync_db
DB_SSL=true

# Reverse Proxy Configuration
# Set to true if behind Nginx, CloudFlare, AWS ALB, etc.
TRUST_PROXY=true

# CORS Configuration - WHITELIST YOUR FRONTEND DOMAINS
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_METHODS=GET,POST,PUT,PATCH,DELETE
CORS_CREDENTIALS=true

# Cookie Security Configuration
# Secure: Only send cookies over HTTPS (auto-enabled in production)
COOKIE_SECURE=true
# HttpOnly: Prevent JavaScript access to cookies (prevent XSS)
COOKIE_HTTP_ONLY=true
# SameSite: CSRF protection (strict recommended)
COOKIE_SAME_SITE=strict
# Cookie domain (optional, for subdomain sharing)
# COOKIE_DOMAIN=.yourdomain.com
# Cookie expiration in milliseconds (default: 1 hour)
COOKIE_MAX_AGE=3600000

# Redis config
REDIS_HOST=redis-14303.c282.east-us-mz.azure.cloud.redislabs.com
REDIS_PORT=14303
REDIS_PASSWORD=your-secure-redis-password
REDIS_DB=0


#Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_secret


# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true

# Global rate limiting (applies to all requests)
# Production default: 50 req/min (stricter)
RATE_LIMIT_GLOBAL_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_MAX=50

# Per-IP rate limiting (unauthenticated users)
# Production default: 10 req/min (very strict)
RATE_LIMIT_PER_IP_WINDOW_MS=60000
RATE_LIMIT_PER_IP_MAX=10

# Per-wallet rate limiting
RATE_LIMIT_PER_WALLET_WINDOW_MS=60000
RATE_LIMIT_PER_WALLET_MAX=50

# Per-user rate limiting (authenticated users)
# Production default: 50 req/min (strict)
RATE_LIMIT_PER_USER_WINDOW_MS=60000
RATE_LIMIT_PER_USER_MAX=50

# Predefined rate limits
RATE_LIMIT_STRICT_MAX=10
RATE_LIMIT_RELAXED_MAX=1000

# Paths to exempt from rate limiting (comma-separated)
RATE_LIMIT_EXEMPT_PATHS=/health,/health/redis

# JWT Configuration - REQUIRED IN PRODUCTION (min 32 characters)
# Generate a strong random secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters-long
JWT_EXPIRES_IN=1h

# Mail Configuration (SMTP)
MAIL_SENDER=noreply@yourdomain.com
MAIL_SUBJECT_PREFIX=[SkillSync]
MAIL_APP_NAME=SkillSync
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_SECURE=false
OTP_TTL_MINUTES=10
OTP_SUBJECT=Your One-Time Password (OTP)

# Swagger UI Configuration
# Set to true to enable in production (NOT RECOMMENDED - use only for debugging)
SWAGGER_ENABLED=false

=======
# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
DATABASE_NAME=skillsync
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false
DATABASE_SSL=false
DATABASE_POOL_SIZE=10

# =============================================================================
# JWT Configuration
# =============================================================================

JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-chars
JWT_EXPIRES_IN=24h
JWT_ACCESS_EXPIRATION=24h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-minimum-32
JWT_REFRESH_EXPIRES_IN=7d
>>>>>>> baad43246a6fc8b870e1b4fd64007446b47c45c5

# =============================================================================
# Redis Configuration
# =============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=skillsync
REDIS_CONNECT_TIMEOUT=10000

# =============================================================================
# Feature Flags
# =============================================================================

FEATURE_ENABLE_SWAGGER=true
FEATURE_ENABLE_CACHE=true
FEATURE_ENABLE_RATE_LIMITING=true
FEATURE_ENABLE_LOGGING=true

# =============================================================================
# Seed Configuration
# =============================================================================

# Set to 'true' to disable automatic seeding on startup
DISABLE_SEED=false

# Admin wallet address for the default admin user
# This should be a valid Ethereum-compatible wallet address
DEFAULT_ADMIN_WALLET=0x742d35Cc6634C0532925a3b844Bc2e7c6A9A8B0F

# =============================================================================
# Logging
# =============================================================================

LOG_LEVEL=log
LOG_FORMAT=simple
